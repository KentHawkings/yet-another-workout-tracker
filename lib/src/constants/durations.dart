class Durations {
  Durations._();

  static const Duration animShort = Duration(milliseconds: 100);

  static const Duration animMedium = Duration(milliseconds: 200);

  static const Duration animLong = Duration(milliseconds: 300);
}
